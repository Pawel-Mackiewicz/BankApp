# MySQL configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bankapp?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=true

# Add logging for debugging database connection
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.zaxxer.hikari=DEBUG

# Security
spring.security.user.name=user
spring.security.user.password=$2a$08$LqJIYeGKJMfFRmeb1dlesOYsvK/49z0.3kfdDCGd0foCIU/mnRQVe

# Optional: If you want to control the naming strategy or dialect explicitly (note: MySQL dialect is usually auto-selected)
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect